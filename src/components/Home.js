import React from 'react'; // Importa React para usar JSX y definir el componente funcional.

const Home = () => { // Declara un componente funcional llamado Home.
  return (
    <div> {/* Contenedor principal del contenido del componente. */}
      <h1>Bienvenido al Inicio</h1> {/* Encabezado principal que se muestra en la página. */}
    </div>
  );
};

export default Home; // Exporta el componente Home para que pueda ser utilizado en otras partes de la aplicación.

